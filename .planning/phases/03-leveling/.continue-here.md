---
context: phase
phase: 03-leveling
task: 2
total_tasks: 2
status: in_progress
last_updated: 2026-05-22T22:01:31.842Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

_No blocking constraints identified this session._

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| First-ever migrate dev | This project had no `prisma/migrations/` directory before Phase 3. `prisma migrate dev` required baselining the existing db-push schema via `migrate diff` + `migrate resolve --applied` before applying the new `maxHp` column. | advisory | If schema changes are needed in future phases, check whether `prisma/migrations/` exists first. If it does, normal `migrate dev` flow applies. |

<current_state>
Phase 3 (Leveling) execution is complete — all 4 plans (03-01 through 03-04) are executed and committed.
All code is merged to `main`. The phase is paused at the **human verification checkpoint** for Plan 03-04 (LevelUpCard UI).

The execute-phase orchestrator is waiting for visual confirmation that the `LevelUpCard` renders correctly in a real game session. Once the user replies "approved", the orchestrator runs code review → verifier agent → marks phase complete.

The user added `npm run db:boost-xp` and "The Proving Grounds" Veteran story to enable fast testing.
</current_state>

<completed_work>
Wave 1 — 03-01: Math + Persistence Foundation ✅
- `lib/leveling.ts` pure module: `maxHpAtLevel`, `HIT_DIE_BY_CLASS`, `proficiencyBonus` re-export
- `lib/leveling.test.ts`: 79 tests (LVL-01 + LVL-05 fixture grid, all 4 classes × 5 levels × 3 CON mods)
- Prisma migration `20260522210049_add_character_max_hp` applied (first-ever migration on this project)
- `prisma/seed-backfill-maxhp.mjs` ran: 6 characters backfilled to class-correct maxHp

Wave 2 — 03-02: Character Creation + Adventure Start ✅
- `create-character.ts`: computes `maxHpAtLevel(class, con, 1)` and stores in `character.maxHp`
- `start-adventure.ts`: line 49 formula replaced with `member.character.maxHp` (DB read)
- 5 TDD behavior specs for LVL-03

Wave 2 — 03-03: take-turn Atomic Level-Up ✅
- `take-turn.ts`: `LevelUpResult` interface, `maxHp: newMaxHp` in `$transaction`, `levelUpResult` return
- LVL-04/D-12 verified: `RULES_ENGINE_KEYS` at line 299 already contains all 5 keys (no code change)
- 3 multi-level-up delta tests (LVL-02 proof)

Wave 3 — 03-04 Task 1: LevelUpCard Component ✅
- `app/game/[id]/page.tsx`: `LevelUpCard` indigo component, state lifecycle, FieldTab prop wiring
- Card shows: `⬆ Level X → Y | Max HP: A → B` + proficiency line only on level-5 transition

Dev testing utilities ✅
- `prisma/dev-boost-xp.mjs` + `npm run db:boost-xp` — pre-stages character XP near threshold
- "The Proving Grounds" Veteran story prompt seeded — Claude instructed for back-to-back combat
</completed_work>

<remaining_work>
**Wave 3 — 03-04 Task 2: Human Visual Verification (PENDING)**
- User must confirm the LevelUpCard renders correctly in a real level-up game turn
- If approved: run code review → verifier → mark phase complete

**Post-verification steps (orchestrator handles these automatically):**
1. Code review gate (`/gsd:code-review 3`)
2. Regression gate (run prior phases' tests)
3. Schema drift gate check
4. Verifier agent creates `03-VERIFICATION.md`
5. `gsd-sdk query phase.complete "3"` marks phase complete
6. `update_project_md` evolves `PROJECT.md`
</remaining_work>

<decisions_made>
- `Character.maxHp` stored as a schema column (not in Game.state) — canonical, persists across games
- `create-character.ts` computes maxHp at creation via `maxHpAtLevel(class, con, 1)`
- `start-adventure.ts` reads canonical `character.maxHp` from DB (no recalculation)
- LevelUpCard uses indigo color family (distinct from amber dice card)
- `levelUpResult` returned ephemerally from `take-turn` server action (not persisted to Message table)
- First-ever migration required `migrate diff` + `migrate resolve --applied` to baseline the existing schema
- Multi-level-up HP uses `maxHpAtLevel(class, con, finalLevel)` total-HP function (not per-level loop)
</decisions_made>

<blockers>
**Human verification checkpoint** — cannot mark 03-04 complete or advance phase until user tests:
1. `npm run db:boost-xp "<charName>" 2` — pre-stage character to 250 XP
2. `npm run dev` → start a game with "The Proving Grounds" (Veteran)
3. Complete one encounter → LevelUpCard should appear above narration in indigo
4. Click next chip → card disappears
5. Reply "approved" in the execute-phase conversation
</blockers>

## Required Reading (in order)
1. `.planning/phases/03-leveling/03-04-PLAN.md` — Task 2 human-verify instructions
2. `.planning/phases/03-leveling/03-04-SUMMARY.md` — what Task 1 built (LevelUpCard details)
3. `.planning/phases/03-leveling/03-03-SUMMARY.md` — `levelUpResult` shape from take-turn

## Infrastructure State
- **Dev server**: NOT running — start with `npm run dev`
- **Database**: Migration applied, backfill complete, "The Proving Grounds" story seeded
- **Tests**: 137 passing, working tree clean
- **Branch**: `main` — all work committed

<context>
Phase 3 execution is essentially done. All code is written, committed, and passing tests. The only remaining step is the user visually confirming the LevelUpCard in the browser, then the automated completion pipeline (code review + verifier) can run. The testing utilities (boost-xp script + Proving Grounds scenario) were added specifically to make this checkpoint fast — one `db:boost-xp` command + two encounters is all it takes.
</context>

<next_action>
Resume the execute-phase orchestrator conversation for Phase 3 and continue from the human verification checkpoint.

Fast path to test:
```bash
npm run db:boost-xp          # list characters and current XP
npm run db:boost-xp "<name>" 2   # set XP to 250 (50 below level 2)
npm run dev                   # start dev server
```
Then: create a game with "The Proving Grounds" (Veteran), complete one encounter, confirm the indigo LevelUpCard appears above the narration. Reply "approved" to continue.
</next_action>
