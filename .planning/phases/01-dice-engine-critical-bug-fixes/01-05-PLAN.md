---
phase: 01-dice-engine-critical-bug-fixes
plan: 05
type: execute
wave: 4
depends_on:
  - 01-04
files_modified:
  - app/game/[id]/page.tsx
autonomous: true
requirements:
  - DICE-02
  - DICE-03
  - DICE-04

must_haves:
  truths:
    - "After a player submits a chip, a dice card appears ABOVE the DM narrative text (per D-01, D-02)"
    - "Dice card format matches exactly: '🎲 14 + 3 = 17  vs AC 14  HIT!' (per D-03)"
    - "The 'vs AC' or 'vs DC' label is dynamic based on D20Result.dcType (per D-04)"
    - "The outcome label shows 'HIT!' for attack rolls (dcType='AC') and 'SUCCESS!'/'FAIL!' for DC rolls"
    - "Dice card is ephemeral — held in React local state, not persisted to DB"
    - "Chronicle tab does NOT show historical dice cards — only the FieldTab Current Situation shows the latest"
    - "When isTakingTurn is true, dice card area shows a loading state or is absent (not stale)"
  artifacts:
    - path: "app/game/[id]/page.tsx"
      provides: "DiceCard component, diceResult state in GamePage, dice card rendered above narrative in FieldTab"
  key_links:
    - from: "app/game/[id]/page.tsx"
      to: "app/actions/take-turn.ts"
      via: "result.diceResult from takeTurn() call"
      pattern: "result\\.diceResult"
    - from: "DiceCard component"
      to: "D20Result interface"
      via: "prop type — DiceCard receives the D20Result object"
      pattern: "D20Result"
---

<objective>
Add a dice result card to the FieldTab's "Current Situation" section that renders above the DM narrative. The card shows the full mechanical result (roll + modifier = total vs AC/DC N — HIT!/SUCCESS!/FAIL!) from D20Result returned by takeTurn.

Purpose: This is the player-visible payoff of the dice engine work in Plans 02-04. Without this UI, the dice engine exists but players never see mechanical roll results — only Claude's narrative. Closing this loop makes the game feel honest and transparent.
Output: DiceCard component in page.tsx; diceResult local state in GamePage; dice card rendered conditionally above narrative in FieldTab.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-dice-engine-critical-bug-fixes/01-CONTEXT.md

<interfaces>
From app/actions/take-turn.ts (after Plan 04 refactor):
  interface TurnResult {
    success:     boolean;
    narrative?:  string;
    chips?:      string[];
    newState?:   Record<string, unknown>;
    diceResult?: D20Result;  // NEW — added in Plan 04
    error?:      string;
  }

From lib/dice.ts (Plan 02):
  export interface D20Result {
    roll:     number;        // 1-20 raw face value
    modifier: number;        // ability modifier applied
    total:    number;        // roll + modifier
    dc:       number;        // difficulty class
    dcType:   "AC" | "DC";  // "AC" for attack rolls, "DC" for skill checks
    success:  boolean;       // total >= dc
    critical: boolean;       // roll === 20
    fumble:   boolean;       // roll === 1
  }

From app/game/[id]/page.tsx current state (read this file fully before editing):
  - Line 109-343: GamePage component with useState for gameData, localMessages, localState, etc.
  - Line 182-216: handleChipClick — calls takeTurn(gameId, chip), handles result.success
  - Line 196-204: result.narrative is used to append to localMessages
  - Line 204: result.newState updates localState
  - Line 317-329: FieldTab rendered with localMessages, chips, etc.
  - Line 347-437: FieldTab component with situationText = lastDm?.content
  - Line 386-395: "Current Situation" div — the dice card goes ABOVE the narrative text at line 390

D-01/D-02/D-03/D-04 exact requirements (from CONTEXT.md):
  D-01: Show BOTH a dice card AND Claude's narrative — not narrative only
  D-02: Dice card appears ABOVE the narrative paragraph
  D-03: Card format: "🎲 14 + 3 = 17  vs AC 14  HIT!" — full math shown including target number
  D-04: Label is DYNAMIC: "vs AC [N]" for attack rolls, "vs DC [N]" for skill checks

FieldTab current signature (lines 347-360):
  function FieldTab({ state, map, storyPrompt, messages, chips, partyMarkers,
    onChipClick, isInitializing, isTakingTurn, chipsEnabled })
  — must add `diceResult?: D20Result | null` to this props interface
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add diceResult state to GamePage and wire through handleChipClick</name>
  <files>app/game/[id]/page.tsx</files>
  <read_first>
    - /Users/jeffwang/Desktop/dnd-async-app/app/game/[id]/page.tsx — read all 586 lines before editing; understand existing state shape and handleChipClick
    - /Users/jeffwang/Desktop/dnd-async-app/app/actions/take-turn.ts — confirm TurnResult.diceResult field is present after Plan 04
    - /Users/jeffwang/Desktop/dnd-async-app/lib/dice.ts — import D20Result type (type-only import)
  </read_first>
  <action>
    Make three targeted changes to page.tsx. Read the full file first.

    CHANGE 1 — Import D20Result type:
    Add to the imports at the top of the file: `import type { D20Result } from '../../../lib/dice'`
    Place this after the existing named import from take-turn.ts. Use type-only import.

    CHANGE 2 — Add diceResult state to GamePage:
    Inside the GamePage component, beside the existing useState declarations, add:
      const [diceResult, setDiceResult] = useState<D20Result | null>(null)

    CHANGE 3 — Update handleChipClick to capture diceResult:
    In the handleChipClick function, after `const result = await takeTurn(gameId, chip)`:
    - On success: call `setDiceResult(result.diceResult ?? null)` alongside the existing setLocalMessages and setLocalState calls
    - On failure (the else branch that removes the optimistic player message): call `setDiceResult(null)` to clear any stale dice result
    - When isTakingTurn is set to true at the top of handleChipClick, also call `setDiceResult(null)` to clear the previous result while loading

    CHANGE 4 — Pass diceResult to FieldTab:
    In the JSX where FieldTab is rendered (around line 317), add `diceResult={diceResult}` as a prop.
    Update the FieldTab function signature and its destructured props to include `diceResult?: D20Result | null`.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>TypeScript compiles without errors. `grep -c "diceResult" app/game/[id]/page.tsx` returns at least 4 (state declaration, setDiceResult calls, FieldTab prop).</done>
</task>

<task type="auto">
  <name>Task 2: Create DiceCard component and render in FieldTab above narrative</name>
  <files>app/game/[id]/page.tsx</files>
  <read_first>
    - /Users/jeffwang/Desktop/dnd-async-app/app/game/[id]/page.tsx — read after Task 1 changes; locate the "Current Situation" div at around line 380-395
    - /Users/jeffwang/Desktop/dnd-async-app/.planning/phases/01-dice-engine-critical-bug-fixes/01-CONTEXT.md — D-03 exact card format, D-04 dynamic label
  </read_first>
  <action>
    CHANGE 1 — Create DiceCard component:
    Add a new function component DiceCard in page.tsx, placed after the FieldTab component definition and before PartyTab. It accepts `{ result }: { result: D20Result }` as props.

    The card renders a single div with these visual elements:
    - Background: bg-amber-50 border border-amber-200 rounded-lg px-3 py-2 (Tailwind — mobile-friendly compact)
    - Content layout: flex items-center gap-2
    - Dice emoji: text-base (🎲)
    - Math string: `${result.roll} + ${result.modifier} = ${result.total}` in text-sm font-mono text-slate-700
    - Target string: `vs ${result.dcType} ${result.dc}` in text-sm text-slate-500
    - Outcome label: if result.success, render the outcome label in green; if fail, in red
      - Outcome label text logic:
        - if result.critical: "CRIT!" (green, bold)
        - else if result.fumble: "FUMBLE!" (red, bold)
        - else if result.success && result.dcType === "AC": "HIT!" (green)
        - else if result.success && result.dcType === "DC": "SUCCESS!" (green)
        - else if !result.success && result.dcType === "AC": "MISS!" (red)
        - else: "FAIL!" (red)

    Exact format example per D-03: "🎲 14 + 3 = 17  vs AC 14  HIT!"

    CHANGE 2 — Render DiceCard above narrative in FieldTab:
    Inside FieldTab, in the "Current Situation" div (the bg-white border rounded-xl p-4 block that currently renders situationText):
    - Add DiceCard ABOVE the narrative paragraph
    - Conditional rendering: only show DiceCard if `diceResult !== null && !isTakingTurn`
    - Show a loading placeholder if `isTakingTurn` (can reuse the existing isInitializing skeleton style: a div with h-6 bg-amber-100 rounded animate-pulse)
    - The narrative paragraph renders below DiceCard, unchanged, with leading-relaxed text-sm text-slate-700

    The structure inside the Current Situation white box should be:
      {isTakingTurn && <div className="h-6 bg-amber-100 rounded animate-pulse mb-2" />}
      {!isTakingTurn && diceResult && <DiceCard result={diceResult} />}
      {!isInitializing && <p className="text-sm text-slate-700 leading-relaxed mt-2">{situationText}</p>}
      {isInitializing && <p ... animate-pulse>...</p>}

    Do NOT show the DiceCard in ChronicleTab — per codebase_reality, historical dice cards are not stored in DB for Phase 1.
    Do NOT change any other tab rendering logic.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    TypeScript compiles without errors.
    `grep -c "DiceCard" app/game/[id]/page.tsx` returns at least 3 (component definition + conditional render + import/use in FieldTab).
    `grep -c "vs.*dcType\|dcType.*vs" app/game/[id]/page.tsx` returns at least 1 (dynamic AC/DC label).
    `npm run build` passes without errors.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify dice card renders correctly in the browser</name>
  <what-built>
    DiceCard component renders in the FieldTab Current Situation box above the DM narrative. It shows the full dice math, dynamic AC/DC label, and color-coded outcome label. State is ephemeral — held in React state, not the DB.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and navigate to an active game at http://localhost:3000/game/{id}
    2. Verify the "Current Situation" box shows the DM narrative text (no dice card yet on load — diceResult is null initially)
    3. Click any action chip to submit a turn
    4. While processing: verify the amber loading skeleton appears in the Current Situation box
    5. After the turn resolves: verify a dice card appears ABOVE the narrative text, formatted like:
         🎲 14 + 3 = 17  vs AC 12  HIT!
       (exact numbers will vary — verify the format and positions)
    6. Verify the outcome label color: green for success (HIT!/SUCCESS!/CRIT!), red for failure (MISS!/FAIL!/FUMBLE!)
    7. Verify the Chronicle tab does NOT show any dice card — only the text narrative appears there
    8. Submit a second chip and verify the dice card updates to the new result (old result is replaced)
  </how-to-verify>
  <resume-signal>Type "dice-card-verified" if the card renders correctly, or describe any visual issues for debugging.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| takeTurn result → diceResult state | D20Result from server flows into client React state for rendering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | D20Result displayed in UI | accept | D20Result is server-computed in rollD20Check — client only renders it, cannot alter it; the number shown is the actual roll |
| T-05-02 | Information Disclosure | Dice roll numbers visible to player | accept | Per D-08, transparent roll results are the intended design — players can see their roll math, which is a game feature not a leak |
| T-05-03 | Denial of Service | Stale diceResult shown after error | mitigate | setDiceResult(null) called at the start of each chip click and on error path — stale result cannot persist across failed turns |
</threat_model>

<verification>
1. `npx tsc --noEmit` exits 0.
2. `npm run build` exits 0 — no compile errors in the Next.js build.
3. `grep -c "DiceCard" app/game/[id]/page.tsx` returns at least 3.
4. `grep -c "diceResult" app/game/[id]/page.tsx` returns at least 5 (state, set calls, prop, condition).
5. `grep -c "dcType" app/game/[id]/page.tsx` returns at least 1 (dynamic label).
6. Human verification: dice card appears above narrative in FieldTab after chip click.
7. Human verification: Chronicle tab shows no dice cards.
</verification>

<success_criteria>
- Dice card renders in FieldTab above narrative immediately after a turn resolves
- Card format: "🎲 {roll} + {modifier} = {total}  vs {AC|DC} {dc}  {outcome}!" matching D-03
- dcType label is dynamic: "vs AC" for attack rolls, "vs DC" for skill checks (D-04)
- Outcome labels: HIT!/MISS! for attack rolls, SUCCESS!/FAIL! for DC rolls, CRIT! / FUMBLE! for critical/fumble
- Green for success, red for failure
- Chronicle tab has no dice cards — ephemeral state only (per Phase 1 scope)
- Loading skeleton shows during processing so stale result is not displayed
- TypeScript and Next.js build pass cleanly
</success_criteria>

<output>
Create `.planning/phases/01-dice-engine-critical-bug-fixes/01-05-SUMMARY.md` when done.
</output>
