---
phase: 01-dice-engine-critical-bug-fixes
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - prisma/schema.prisma
autonomous: false
requirements:
  - DICE-04

must_haves:
  truths:
    - "Game model in prisma/schema.prisma has a version Int @default(0) field"
    - "Database migration ran successfully — the version column exists in the live DB"
    - "Prisma client was regenerated and Game type includes the version field"
    - "Existing game rows have version = 0 (safe default)"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Game model with version Int @default(0)"
      contains: "version Int @default(0)"
    - path: "prisma/migrations/"
      provides: "Migration history baseline + add-game-version migration"
  key_links:
    - from: "prisma/schema.prisma"
      to: "lib/prisma.ts"
      via: "npx prisma generate regenerates the client"
      pattern: "version.*Int.*@default"
    - from: "prisma/migrations/"
      to: "Game table in Supabase/Neon PostgreSQL"
      via: "npx prisma migrate dev"
      pattern: "ALTER TABLE.*game.*ADD COLUMN.*version"
---

<objective>
[BLOCKING] Add Game.version column (optimistic lock sentinel) to prisma/schema.prisma and apply the migration to the live database. This unblocks Plan 04 which requires game.version to implement the STALE_TURN race condition fix.

Purpose: The non-atomic Promise.all in take-turn.ts cannot be fixed without the version column. Without it, two concurrent submissions can silently corrupt game state. This plan creates the column with a safe default so existing rows are unaffected.
Output: prisma/schema.prisma updated; migration applied; Prisma client regenerated with Game.version field available.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-dice-engine-critical-bug-fixes/01-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md

<interfaces>
Current Game model in prisma/schema.prisma (relevant excerpt — no version column):
  model Game {
    id            String      @id @default(uuid())
    characterId   String
    character     Character   @relation(...)
    storyPromptId String
    storyPrompt   StoryPrompt @relation(...)
    mapId         String
    map           Map         @relation(...)
    status        GameStatus  @default(ACTIVE)
    phase         GamePhase   @default(ACTIVE)
    currentTurnCharacterId String?
    state         Json
    createdAt     DateTime    @default(now())
    updatedAt     DateTime    @updatedAt
    messages      Message[]
    partyMembers  PartyMember[]
    // version column does NOT exist yet
  }

After this plan, Game will have:
    version Int @default(0)    // optimistic lock counter — increment on every state write

Migration context:
  No prisma/migrations/ directory exists (schema was applied via db push).
  Primary command: npx prisma migrate dev --name add-game-version
  Fallback (non-interactive): npx prisma db push
  After migration: npx prisma generate
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add version field to Game model in schema.prisma</name>
  <files>prisma/schema.prisma</files>
  <read_first>
    - /Users/jeffwang/Desktop/dnd-async-app/prisma/schema.prisma — read the entire file to see the current Game model before editing
  </read_first>
  <action>
    In prisma/schema.prisma, locate the Game model. Add exactly one new field after the `state Json` line and before `createdAt`:

      version Int @default(0)   // optimistic lock counter

    The comment is important — it documents why this field exists for future developers. Do not change any other field, enum, or model. Do not alter the datasource or generator blocks.

    After editing, verify the schema is syntactically valid by running `npx prisma validate`.
  </action>
  <verify>
    <automated>npx prisma validate && grep -c "version Int @default(0)" prisma/schema.prisma</automated>
  </verify>
  <done>prisma validate exits 0 and `grep -c "version Int @default(0)" prisma/schema.prisma` returns 1.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>[BLOCKING] Task 2: Run database migration and regenerate Prisma client</name>
  <what-built>
    Schema edit is complete. Now the migration must be applied to the live Supabase/Neon database and the Prisma client must be regenerated so Game.version is available as a typed field in TypeScript.
  </what-built>
  <how-to-verify>
    Run the following commands in sequence. If migrate dev prompts interactively, use the db push fallback instead.

    Primary (creates migration history):
      npx prisma migrate dev --name add-game-version

    Fallback if migrate dev hangs or errors with interactive prompt:
      npx prisma db push

    After whichever command succeeds:
      npx prisma generate

    Then verify:
    1. Run `npx prisma studio` or check the Supabase dashboard — the Game table should have a `version` column with default 0.
    2. Confirm existing rows have version = 0 (no nulls).
    3. Run `grep -r "version" node_modules/.prisma/client/index.d.ts 2>/dev/null | grep "Game" | head -5` — should show version field in Game type definition.

    If the migration fails with an error about baseline:
      - Run: `npx prisma migrate dev --name init` first (creates baseline from current state)
      - Then: `npx prisma migrate dev --name add-game-version`
  </how-to-verify>
  <resume-signal>Type "migration-done" once the migration applied successfully and npx prisma generate completed without errors. Describe any errors encountered.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer machine → Neon/Supabase PostgreSQL | Migration command writes DDL to the live database |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Game.version column default | accept | @default(0) is safe — existing rows will receive 0, which is a valid initial version value; no data loss |
| T-03-02 | Denial of Service | Migration applied to live DB | mitigate | `version Int @default(0)` is an additive change only — no existing columns are altered or dropped; rollback risk is minimal |
| T-03-03 | Elevation of Privilege | Prisma migrate dev running with DB credentials | accept | Credentials are in .env.local (not committed); migration is additive only; developer is running this in their own environment |
</threat_model>

<verification>
1. `grep -c "version Int @default(0)" prisma/schema.prisma` returns 1.
2. `npx prisma validate` exits 0.
3. `npx prisma generate` exits 0 after migration (Prisma client includes version field).
4. Human confirms: Game table in Supabase/Neon has version column with default 0.
5. Human confirms: existing game rows have version = 0 (not NULL).
</verification>

<success_criteria>
- prisma/schema.prisma has version Int @default(0) in the Game model
- Migration applied to live database without errors
- Prisma client regenerated — TypeScript code can access game.version as a typed number
- No existing game rows broken (version = 0 for all pre-existing rows)
</success_criteria>

<output>
Create `.planning/phases/01-dice-engine-critical-bug-fixes/01-03-SUMMARY.md` when done.
</output>
