---
phase: 04-skills-abilities-integration
plan: 03
type: tdd
wave: 1
depends_on: []
files_modified:
  - lib/skills.ts
  - lib/skills.test.ts
autonomous: true

requirements:
  - SKILL-03
  - SKILL-05

must_haves:
  truths:
    - "resolveSkillCheck returns a full SkillCheckResult with all 9 fields"
    - "Proficient character total = d20 roll + abilityModifier(abilityScore) + proficiencyBonus(level)"
    - "Non-proficient character total = d20 roll + abilityModifier(abilityScore) only (no proficiency bonus)"
    - "All 18 SKILL_ABILITY_MAP entries map to the correct ability score"
    - "DC boundary: total === dc is SUCCESS; total === dc-1 is FAILURE"
    - "Unknown skill name throws Error with message containing the bad name"
    - "npm run test:run passes with 0 failures"
  artifacts:
    - path: "lib/skills.ts"
      provides: "SKILL_ABILITY_MAP, CLASS_SKILL_POOL, SKILL_PICK_COUNT, resolveSkillCheck, SkillCheckResult"
      exports: ["SKILL_ABILITY_MAP", "CLASS_SKILL_POOL", "SKILL_PICK_COUNT", "resolveSkillCheck"]
    - path: "lib/skills.test.ts"
      provides: "unit tests covering SKILL-03 and SKILL-05 scenarios"
      min_lines: 80
  key_links:
    - from: "lib/skills.ts"
      to: "lib/dice.ts"
      via: "import { abilityModifier, proficiencyBonus, rollDie } from './dice'"
      pattern: "from.*dice"
---

<objective>
Create lib/skills.ts as a pure TypeScript module following the lib/leveling.ts pattern, exporting SKILL_ABILITY_MAP (18 entries), CLASS_SKILL_POOL, SKILL_PICK_COUNT, and resolveSkillCheck. Create lib/skills.test.ts with full TDD coverage for SKILL-03 and SKILL-05.

Purpose: lib/skills.ts is the shared dependency for create-character.ts (validation) and take-turn.ts (skill check resolution). Being a pure function with injectable rollFn makes it fully testable in isolation and safe to import from any server action or component.

Output: lib/skills.ts and lib/skills.test.ts; all tests pass under npm run test:run.
</objective>

<execution_context>
@/Users/jeffwang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeffwang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-skills-abilities-integration/04-RESEARCH.md
@.planning/phases/04-skills-abilities-integration/04-PATTERNS.md
</context>

<feature>
  <name>lib/skills.ts pure module + lib/skills.test.ts unit tests</name>
  <files>lib/skills.ts, lib/skills.test.ts</files>
  <behavior>
    RED phase — write tests first, run them, all FAIL:

    Test group 1: SKILL_ABILITY_MAP constant
    - SKILL_ABILITY_MAP["Stealth"] === "dexterity"
    - SKILL_ABILITY_MAP["Athletics"] === "strength"
    - SKILL_ABILITY_MAP["Arcana"] === "intelligence"
    - SKILL_ABILITY_MAP["Insight"] === "wisdom"
    - SKILL_ABILITY_MAP["Intimidation"] === "charisma"
    - Object.keys(SKILL_ABILITY_MAP).length === 18
    (These 5 spot-checks + length test cover all 18 mappings efficiently)

    Test group 2: CLASS_SKILL_POOL and SKILL_PICK_COUNT
    - CLASS_SKILL_POOL["Fighter"].length === 8
    - CLASS_SKILL_POOL["Rogue"].length === 11
    - CLASS_SKILL_POOL["Cleric"].length === 5
    - CLASS_SKILL_POOL["Wizard"].length === 6
    - SKILL_PICK_COUNT["Fighter"] === 2
    - SKILL_PICK_COUNT["Rogue"] === 4
    - SKILL_PICK_COUNT["Cleric"] === 2
    - SKILL_PICK_COUNT["Wizard"] === 2

    Test group 3: resolveSkillCheck — proficient character
    - Stealth check, Rogue level 1, DEX 14 (mod +2), proficient, DC 12, rollFn returns 10:
      result.proficient === true
      result.roll === 10
      result.modifier === 2
      result.proficiencyBonus === 2 (proficiencyBonus(1) = 2)
      result.total === 14 (10 + 2 + 2)
      result.success === true (14 >= 12)
      result.dc === 12
      result.abilityScore === "dexterity"
      result.skill === "Stealth"

    Test group 4: resolveSkillCheck — non-proficient character
    - Stealth check, Fighter level 1, DEX 14 (mod +2), NOT proficient (skillProficiencies: []), DC 12, rollFn returns 10:
      result.proficient === false
      result.proficiencyBonus === 0
      result.total === 12 (10 + 2 + 0)
      result.success === true (12 >= 12) — DC boundary: exactly AT DC is success

    Test group 5: DC boundary conditions
    - DC 12, total 11 (rollFn returns 9, mod 2, no proficiency): result.success === false (11 < 12)
    - DC 12, total 12: result.success === true
    - DC 12, total 13: result.success === true

    Test group 6: Unknown skill throws
    - resolveSkillCheck("Lockpicking", {...}, 12) throws Error matching /Unknown skill: Lockpicking/

    GREEN phase — implement lib/skills.ts so all tests pass.

    REFACTOR phase — ensure exports are clean, header matches lib/leveling.ts style, no dead code.
  </behavior>
  <implementation>
    lib/skills.ts must be created with:

    Header comment block matching lib/leveling.ts style (// ─── Skills Engine ───... / Pure TypeScript / Zero framework / Zero Prisma).

    Import: import { abilityModifier, proficiencyBonus, rollDie } from "./dice"
    (All three are already exported from lib/dice.ts per Phase 1)

    Export interface SkillCharacterInput (NOT exported from character-sheet.ts — defined locally):
      characterClass: string
      level: number
      strength: number
      dexterity: number
      constitution: number
      intelligence: number
      wisdom: number
      charisma: number
      skillProficiencies: string[]

    Export interface SkillCheckResult:
      skill: string
      abilityScore: string
      roll: number
      modifier: number
      proficiencyBonus: number
      total: number
      dc: number
      success: boolean
      proficient: boolean

    Export const SKILL_ABILITY_MAP: Record<string, keyof SkillCharacterInput> — 18 entries extracted from lib/character-sheet.ts SKILLS array (lines 87-106); READ that file to get exact values, do NOT recreate from memory:
      Acrobatics→dexterity, Animal Handling→wisdom, Arcana→intelligence, Athletics→strength,
      Deception→charisma, History→intelligence, Insight→wisdom, Intimidation→charisma,
      Investigation→intelligence, Medicine→wisdom, Nature→intelligence, Perception→wisdom,
      Performance→charisma, Persuasion→charisma, Religion→intelligence, Sleight of Hand→dexterity,
      Stealth→dexterity, Survival→wisdom

    Export const CLASS_SKILL_POOL: Record<string, string[]>:
      Fighter: ["Acrobatics","Animal Handling","Athletics","History","Insight","Intimidation","Perception","Survival"]
      Rogue: ["Acrobatics","Athletics","Deception","Insight","Intimidation","Investigation","Perception","Performance","Persuasion","Sleight of Hand","Stealth"]
      Cleric: ["History","Insight","Medicine","Persuasion","Religion"]
      Wizard: ["Arcana","History","Insight","Investigation","Medicine","Religion"]

    Export const SKILL_PICK_COUNT: Record<string, number>:
      Fighter: 2, Rogue: 4, Cleric: 2, Wizard: 2

    Export function resolveSkillCheck(skillName: string, character: SkillCharacterInput, dc: number, rollFn?: () => number): SkillCheckResult:
      - const abilityKey = SKILL_ABILITY_MAP[skillName]; if (!abilityKey) throw new Error(`Unknown skill: ${skillName}`)
      - const roll = rollFn ? rollFn() : rollDie(20)
      - const abilityScore = character[abilityKey] as number
      - const modifier = abilityModifier(abilityScore)
      - const proficient = character.skillProficiencies.includes(skillName)
      - const profBonus = proficient ? proficiencyBonus(character.level) : 0
      - const total = roll + modifier + profBonus
      - return { skill: skillName, abilityScore: abilityKey, roll, modifier, proficiencyBonus: profBonus, total, dc, success: total >= dc, proficient }

    lib/skills.test.ts must:
    - Import { describe, it, expect } from 'vitest'
    - Import { resolveSkillCheck, SKILL_ABILITY_MAP, CLASS_SKILL_POOL, SKILL_PICK_COUNT } from './skills'
    - Follow the header comment and section separator style from lib/leveling.test.ts
    - Use the injectable rollFn parameter: resolveSkillCheck("Stealth", char, 12, () => 10)
    - Cover ALL behavior groups listed above
  </implementation>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lib/skills.ts → lib/dice.ts | Pure function import; no external calls; no network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Tampering | resolveSkillCheck unknown skill name | mitigate | throw new Error(`Unknown skill: ${skillName}`) — callers (take-turn.ts) validate against SKILL_ABILITY_MAP keys before calling; unit test confirms throw behavior |
| T-04-03-02 | Tampering | rollFn injection in tests | accept | rollFn is a test-only override; production call uses rollDie(20) from the dice engine; no user-controlled path to inject rollFn at runtime |
| T-04-03-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages; Vitest is already installed |
</threat_model>

<verification>
1. `npm run test:run -- lib/skills.test.ts` exits 0 with all tests passing
2. `npm run test:run` (full suite) exits 0 — no regressions in existing tests
3. `grep -c "export" lib/skills.ts` is >= 5 (SKILL_ABILITY_MAP, CLASS_SKILL_POOL, SKILL_PICK_COUNT, resolveSkillCheck, SkillCheckResult, SkillCharacterInput)
4. `grep -c "Prisma\|prisma\|import.*next\|import.*react" lib/skills.ts` returns 0 (pure module)
</verification>

<success_criteria>
- lib/skills.ts exports SKILL_ABILITY_MAP (18 entries), CLASS_SKILL_POOL, SKILL_PICK_COUNT, resolveSkillCheck, SkillCheckResult, SkillCharacterInput
- lib/skills.test.ts covers all 6 behavior groups: constant smoke tests, class pool lengths, proficient check, non-proficient check, DC boundary, unknown skill throw
- npm run test:run passes with 0 failures, 0 errors
- lib/skills.ts has zero Prisma, Next.js, or React imports
</success_criteria>

<output>
Create `.planning/phases/04-skills-abilities-integration/04-03-SUMMARY.md` when done
</output>
