---
phase: 04-skills-abilities-integration
plan: 05
type: execute
wave: 3
depends_on:
  - 04-01-PLAN
  - 04-04-PLAN
files_modified:
  - lib/character-sheet.ts
autonomous: true

requirements:
  - SKILL-01
  - SKILL-02
  - SKILL-03
  - SKILL-04
  - SKILL-05

must_haves:
  truths:
    - "MemberStatsPane shows proficiency highlights derived from Character.skillProficiencies in the DB, not from SKILL_PROFS fallback"
    - "The SKILL_PROFS hardcoded constant is deleted from lib/character-sheet.ts"
    - "CharacterInput interface in lib/character-sheet.ts accepts skillProficiencies?: string[]"
    - "getCharacterSheetData uses char.skillProficiencies ?? [] instead of SKILL_PROFS[char.characterClass] ?? []"
    - "A Fighter with Athletics and Intimidation in their DB skillProficiencies sees those skills highlighted in the Stats sub-tab"
  artifacts:
    - path: "lib/character-sheet.ts"
      provides: "CharacterInput with skillProficiencies?, getCharacterSheetData using live DB data, SKILL_PROFS deleted"
      contains: "skillProficiencies"
  key_links:
    - from: "app/game/[id]/page.tsx MemberStatsPane"
      to: "lib/character-sheet.ts getCharacterSheetData"
      via: "getCharacterSheetData({ ...char, skillProficiencies: char.skillProficiencies })"
      pattern: "getCharacterSheetData"
    - from: "lib/character-sheet.ts getCharacterSheetData"
      to: "char.skillProficiencies"
      via: "new Set<string>(char.skillProficiencies ?? [])"
      pattern: "skillProficiencies"
---

<objective>
Replace the SKILL_PROFS hardcoded fallback in lib/character-sheet.ts with live per-character data from Character.skillProficiencies. CharacterInput grows an optional skillProficiencies field; getCharacterSheetData uses it directly. MemberStatsPane in page.tsx passes char.skillProficiencies (now available after 04-01 extended CharacterData) through to getCharacterSheetData automatically via structural typing.

Purpose: Completes the data flow from DB → character-sheet → UI for skill proficiency display. After this plan, the Stats sub-tab accurately reflects each character's chosen skills, not a class-level hardcoded default.

Output: lib/character-sheet.ts with SKILL_PROFS deleted, CharacterInput extended, and getCharacterSheetData line 113 updated.
</objective>

<execution_context>
@/Users/jeffwang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeffwang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-skills-abilities-integration/04-RESEARCH.md
@.planning/phases/04-skills-abilities-integration/04-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove SKILL_PROFS fallback; extend CharacterInput; wire skillProficiencies in getCharacterSheetData</name>
  <read_first>
    - lib/character-sheet.ts — read in full (134 lines); pay attention to:
      - CharacterInput interface (lines 7-16) — add skillProficiencies?: string[] here
      - SKILL_PROFS constant (lines 60-73) — delete this entire block including any preceding comment
      - getCharacterSheetData function (lines 110-133) — specifically line 113 where SKILL_PROFS is referenced
    - app/game/[id]/page.tsx lines 45-59 — CharacterData interface (has skillProficiencies: string[] after 04-01); lines 762-873 — MemberStatsPane, specifically how char is passed to getCharacterSheetData (line 763)
    - lib/skills.ts — confirm SKILL_ABILITY_MAP keys match the SKILLS array in character-sheet.ts (cross-check; do not change the SKILLS array in character-sheet.ts)
  </read_first>
  <behavior>
    - Test 1 (structural): After edits, lib/character-sheet.ts does not contain the string "SKILL_PROFS"
    - Test 2 (unit via build): getCharacterSheetData called with { ...char, skillProficiencies: ["Athletics"] } returns sheet.skills with Athletics showing proficient: true
    - Test 3 (unit via build): getCharacterSheetData called without skillProficiencies (skillProficiencies: undefined) returns sheet.skills with no proficient skills (falls back to empty array)
    - Test 4 (build gate): npm run build exits 0
  </behavior>
  <action>
    In lib/character-sheet.ts:

    1. Add skillProficiencies?: string[] to the CharacterInput interface, after the charisma field (or wherever the interface ends). It is optional (?) because existing call sites that do not pass skillProficiencies must still work without error.

    2. Delete the SKILL_PROFS constant block (lines 60-73). Include any comment line that introduces it (e.g., "// Default skill proficiency picks..." or similar). Do NOT delete the SKILLS array (lines 87-106) — that stays.

    3. In getCharacterSheetData, find line 113 (approximately):
       BEFORE: `const skillProfs = new Set<string>(SKILL_PROFS[char.characterClass] ?? []);`
       AFTER: `const skillProfs = new Set<string>(char.skillProficiencies ?? []);`
       This single line change is the entire functional change in getCharacterSheetData.

    In app/game/[id]/page.tsx:

    4. Find MemberStatsPane (lines 762-873). Locate the call to getCharacterSheetData on approximately line 763: `const sheet = getCharacterSheetData(char)`. The char argument is already a CharacterData which has skillProficiencies: string[] after 04-01. Because TypeScript uses structural typing and CharacterData is a superset of CharacterInput (with skillProficiencies added to both), this call passes skillProficiencies automatically. Verify the call still compiles — if TypeScript errors because CharacterData has fields not in CharacterInput, cast: getCharacterSheetData(char as CharacterInput). Do NOT restructure MemberStatsPane otherwise.

    No other changes to page.tsx in this plan.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - lib/character-sheet.ts does NOT contain the string "SKILL_PROFS" (grep -c "SKILL_PROFS" lib/character-sheet.ts returns 0)
    - lib/character-sheet.ts CharacterInput interface contains the field skillProficiencies?: string[]
    - lib/character-sheet.ts getCharacterSheetData contains `char.skillProficiencies ?? []` (not SKILL_PROFS)
    - lib/character-sheet.ts SKILLS array (18 entries) is still present and unchanged
    - npm run build exits 0
    - npm run test:run exits 0 (no regressions; the existing test suite still passes)
  </acceptance_criteria>
  <done>SKILL_PROFS deleted; CharacterInput accepts skillProficiencies; getCharacterSheetData reads live DB data; MemberStatsPane passes char.skillProficiencies through structural typing; build and tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end Stats sub-tab proficiency verification</name>
  <what-built>
    All 5 plans of Phase 04 are now complete. The Stats sub-tab should display proficiency highlights sourced from Character.skillProficiencies in the DB, not the old hardcoded SKILL_PROFS fallback.
  </what-built>
  <how-to-verify>
    1. Start the dev server: `npm run dev`
    2. Open the game page for a game with at least one active character.
    3. Navigate to Party tab → Stats sub-tab for a Fighter character.
    4. Confirm: the skills Athletics and Intimidation are highlighted as proficient (green background or checkmark — whatever the existing proficiency highlight style is). No other Fighter skills should be highlighted unless the character was created post-04-01 with different picks.
    5. Confirm: a Rogue character shows Stealth and Perception as proficient (backfill defaults for existing Rogues).
    6. Create a NEW character via the character creation flow:
       - Select Fighter, choose Acrobatics and History (different from backfill defaults)
       - Complete creation
       - Start or join a game with this character
       - Navigate to Stats sub-tab — confirm ONLY Acrobatics and History are highlighted (not Athletics/Intimidation)
    7. Optional: check the Abilities sub-tab for any character — confirm features load from DB (not empty/error) and "New" badge appears on features at the character's current level.
  </how-to-verify>
  <resume-signal>Type "verified" if all checks pass, or describe which check failed.</resume-signal>
  <acceptance_criteria>
    - Stats sub-tab shows correct proficiency highlights for backfilled characters (thematic defaults)
    - A newly created character shows ONLY their chosen skills highlighted in the Stats sub-tab
    - No TypeScript errors in the browser console
    - Abilities sub-tab loads features from DB (not empty, not hardcoded)
  </acceptance_criteria>
  <done>Stats sub-tab proficiency highlight confirmed end-to-end as DB-driven; Phase 04 all 5 plans verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CharacterData.skillProficiencies → getCharacterSheetData | Data flows from DB via Prisma through CharacterData; no user-controlled transformation in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05-01 | Tampering | skillProficiencies?: string[] optional field | accept | Optional field with ?? [] fallback means missing data renders zero proficiencies (conservative default); no security risk — UI display only |
| T-04-05-02 | Information Disclosure | Skills proficiency visible to other party members | accept | All party stats are visible in the Party tab by design; this is cooperative gameplay data |
| T-04-05-03 | Tampering | Residual SKILL_PROFS references elsewhere | mitigate | Verify grep -c "SKILL_PROFS" across the repo returns 0 after deletion; TypeScript build catches any missed references at compile time |
| T-04-05-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages in this plan |
</threat_model>

<verification>
1. `grep -rn "SKILL_PROFS" lib/ app/` returns 0 matches
2. `npm run build` exits 0
3. `npm run test:run` exits 0 (all tests pass including lib/skills.test.ts from 04-03)
4. Human verify: Stats sub-tab shows correct proficiency highlights driven by Character.skillProficiencies (see Task 2)
5. Human verify: creating a new character with non-default skill picks reflects those exact picks in the Stats sub-tab
</verification>

<success_criteria>
PHASE 04 COMPLETE when:
- Character.skillProficiencies String[] exists in DB (04-01)
- Character creation saves class-gated skill picks (04-01)
- Abilities sub-tab fetches ClassFeature from DB; CLASS_FEATURES deleted (04-02)
- lib/skills.ts + lib/skills.test.ts: 18-entry SKILL_ABILITY_MAP; resolveSkillCheck; all tests pass (04-03)
- take-turn.ts: two-call skill check; MECHANICAL CONTEXT keyword; raw numbers never reach Claude (04-04)
- lib/character-sheet.ts: SKILL_PROFS deleted; live skillProficiencies from DB (04-05)
- Stats sub-tab proficiency highlights are DB-driven for all characters
- npm run build exits 0; npm run test:run passes with 0 failures
</success_criteria>

<output>
Create `.planning/phases/04-skills-abilities-integration/04-05-SUMMARY.md` when done
</output>
