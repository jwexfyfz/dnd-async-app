---
phase: 02-xp-system
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - lib/xp.ts
  - lib/xp.test.ts
autonomous: false
requirements:
  - XP-01
  - XP-02
  - XP-05
must_haves:
  truths:
    - "prisma/schema.prisma contains xp Int @default(0) and level Int @default(1) on the Character model"
    - "npm run db:push exits 0 and the Character table in Neon has xp and level columns"
    - "npx prisma generate regenerates the client with xp and level typed on Character"
    - "lib/xp.ts exports XP_THRESHOLDS, XP_BY_DIFFICULTY, computeLevel, and xpForNextLevel with zero Prisma imports"
    - "npm run test:run -- lib/xp.test.ts passes all 10 computeLevel boundary cases and all 5 xpForNextLevel cases"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Character model with xp and level fields"
      contains: "xp    Int @default(0)"
    - path: "lib/xp.ts"
      provides: "XP engine — thresholds, difficulty table, computeLevel, xpForNextLevel"
      exports: ["XP_THRESHOLDS", "XP_BY_DIFFICULTY", "computeLevel", "xpForNextLevel"]
    - path: "lib/xp.test.ts"
      provides: "15 unit tests covering all threshold boundaries"
      contains: "computeLevel"
  key_links:
    - from: "lib/xp.ts"
      to: "lib/xp.test.ts"
      via: "named imports"
      pattern: "import.*computeLevel.*xpForNextLevel.*XP_THRESHOLDS.*from.*'./xp'"
    - from: "prisma/schema.prisma"
      to: "generated/prisma/client"
      via: "npx prisma generate"
      pattern: "xp.*Int.*default"
---

<objective>
Add schema columns xp and level to Character, create the pure XP engine module lib/xp.ts, and validate it with a full unit test suite covering all threshold boundary values.

Purpose: Every downstream plan in this phase depends on the Prisma client knowing about xp/level (Wave 2 take-turn.ts writes) and on lib/xp.ts being available (Wave 2 imports computeLevel and XP_BY_DIFFICULTY). This wave must be complete and verified before Wave 2 begins.

Output: prisma/schema.prisma updated + pushed + client regenerated; lib/xp.ts (pure module); lib/xp.test.ts (15 tests, all passing).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-xp-system/02-CONTEXT.md
@.planning/phases/02-xp-system/02-RESEARCH.md
</context>

<interfaces>
<!-- Key contracts for this plan. No codebase exploration needed. -->

From prisma/schema.prisma — Character model (insert xp and level AFTER charisma):
  charisma     Int @default(8)
  xp           Int @default(0)   ← NEW (add here)
  level        Int @default(1)   ← NEW (add here)

From lib/dice.ts — pure module pattern to replicate exactly:
  // Zero Prisma imports, zero framework imports
  // File-level comment block with ─── separator style
  // All exports are named exports (no default export)
  // Functions are pure — same input always produces same output (or use injectable rollFn)

From lib/dice.test.ts — test pattern to replicate exactly:
  import { describe, it, expect } from 'vitest'   // explicit imports, globals: false
  import { functionName } from './moduleName'      // relative import, no extension
  describe('functionName', () => {
    it('describes the specific case', () => {
      expect(fn(input)).toBe(expected)
    })
  })
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add xp and level to Character schema, push, and regenerate client</name>
  <files>prisma/schema.prisma</files>
  <read_first>
    - prisma/schema.prisma — read the full Character model block to find the exact insertion point (after charisma field, before the relations section)
  </read_first>
  <behavior>
    - After edit: grep -v '^//' prisma/schema.prisma | grep -c "xp.*Int.*default(0)" returns 1
    - After edit: grep -v '^//' prisma/schema.prisma | grep -c "level.*Int.*default(1)" returns 1
    - npm run db:push exits 0 (additive columns, no data loss)
    - npx prisma generate exits 0
  </behavior>
  <action>
    Edit prisma/schema.prisma: inside the Character model block, insert two lines immediately after the charisma field and before the relations (games, partyMemberships):

      xp    Int @default(0)
      level Int @default(1)

    Use the same indentation style as the existing stat fields (two spaces before the field name, aligned type).

    Then run in sequence:
      1. npm run db:push   (NOT npx prisma db push — this project uses the npm script alias per STACK.md)
      2. npx prisma generate

    If npm run db:push prompts for confirmation, the flag --accept-data-loss is NOT needed — this is an additive change only. The command should succeed without flags.

    This task requires human checkpoint because db:push is an irreversible DB operation and must be confirmed.
  </action>
  <verify>
    <automated>grep -v '^//' prisma/schema.prisma | grep -c "xp.*Int.*default" && grep -v '^//' prisma/schema.prisma | grep -c "level.*Int.*default"</automated>
    <human-check>Confirm npm run db:push output shows "Your database is now in sync with your Prisma schema" (or equivalent success message). Confirm npx prisma generate exits 0.</human-check>
  </verify>
  <done>prisma/schema.prisma contains both fields; Neon DB Character table has xp and level columns; Prisma client is regenerated and TypeScript types include xp: number and level: number on Character.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create lib/xp.ts — pure XP engine module</name>
  <files>lib/xp.ts</files>
  <read_first>
    - lib/dice.ts — read fully to internalize the exact file structure, comment separator style, JSDoc style, and export pattern to replicate
    - .planning/phases/02-xp-system/02-RESEARCH.md — XP-02 section: exact algorithm for computeLevel, xpForNextLevel, and constant declarations
  </read_first>
  <behavior>
    - computeLevel(0) returns 1
    - computeLevel(299) returns 1
    - computeLevel(300) returns 2
    - computeLevel(899) returns 2
    - computeLevel(900) returns 3
    - computeLevel(2699) returns 3
    - computeLevel(2700) returns 4
    - computeLevel(6499) returns 4
    - computeLevel(6500) returns 5
    - computeLevel(99999) returns 5 (cap does not exceed 5)
    - xpForNextLevel(1) returns 300
    - xpForNextLevel(2) returns 900
    - xpForNextLevel(3) returns 2700
    - xpForNextLevel(4) returns 6500
    - xpForNextLevel(5) returns null
    - XP_BY_DIFFICULTY["Beginner"] === 50, ["Standard"] === 100, ["Veteran"] === 200
    - File has zero imports from Prisma, Supabase, Next.js, or any framework
  </behavior>
  <action>
    Create lib/xp.ts as a pure TypeScript module. Follow the dice.ts file structure exactly:
    - Open with the ─── section comment block identifying the module (// ─── XP Engine ─────...)
    - Include a module-level comment noting zero framework dependencies and zero Prisma imports
    - No imports at the top of the file (no import statements at all)

    Export four items in this order:

    1. XP_THRESHOLDS: readonly number[] — the array [0, 300, 900, 2700, 6500].
       Index 0 = level 1 start (0 XP). Index N = cumulative XP required for level N+1.
       Source authority: REQUIREMENTS.md XP-02.

    2. XP_BY_DIFFICULTY: Record<string, number> — { Beginner: 50, Standard: 100, Veteran: 200 }.
       Source authority: CONTEXT.md D-04.

    3. computeLevel(xp: number): number — iterates XP_THRESHOLDS from index 1 upward; for each
       threshold[i], if xp >= threshold[i], level = i + 1; break as soon as xp < threshold[i].
       Returns level (minimum 1, maximum 5). Use a for loop, not Array methods.

    4. xpForNextLevel(level: number): number | null — if level >= XP_THRESHOLDS.length (i.e., >= 5),
       return null (at cap). Otherwise return XP_THRESHOLDS[level], which is the cumulative XP
       threshold for the next level.

    Do NOT include fenced code blocks in this action. The exact algorithm logic is in 02-RESEARCH.md
    under "computeLevel Algorithm" and "xpForNextLevel" sections — use those as the implementation
    guide when writing the file.
  </action>
  <verify>
    <automated>npx tsc --noEmit && node -e "const {computeLevel,xpForNextLevel}=require('./lib/xp');console.log(computeLevel(300)===2,computeLevel(6500)===5,xpForNextLevel(5)===null)"</automated>
  </verify>
  <done>lib/xp.ts exists, exports all four items, has zero imports, and npx tsc --noEmit passes. computeLevel(300) === 2, computeLevel(6500) === 5, xpForNextLevel(5) === null confirmed by node check.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create lib/xp.test.ts — full boundary test suite (RED then GREEN)</name>
  <files>lib/xp.test.ts</files>
  <read_first>
    - lib/dice.test.ts — read fully; replicate the import style, describe/it/expect pattern, and file layout exactly
    - lib/xp.ts — read to ensure imports match the exports you just created
    - vitest.config.ts — verify globals setting (expected: false, meaning explicit imports required)
  </read_first>
  <behavior>
    - 10 computeLevel boundary cases pass: inputs (0,299,300,301,899,900,2699,2700,6499,6500) → levels (1,1,2,2,2,3,3,4,4,5)
    - 2 computeLevel edge cases pass: input 99999 → 5, input 0 → 1 (already in boundaries)
    - 5 xpForNextLevel cases pass: levels 1–4 return (300,900,2700,6500) and level 5 returns null
    - npm run test:run -- lib/xp.test.ts exits 0 with all tests passing
    - File uses explicit imports (import { describe, it, expect } from 'vitest') — no global usage
  </behavior>
  <action>
    Create lib/xp.test.ts following the dice.test.ts pattern exactly:
    - Line 1: import { describe, it, expect } from 'vitest'
    - Line 2: import { computeLevel, xpForNextLevel, XP_THRESHOLDS, XP_BY_DIFFICULTY } from './xp'

    Write two describe blocks:

    describe('computeLevel') — 12 test cases:
      All 10 required boundary values from REQUIREMENTS.md XP-05:
        0→1, 299→1, 300→2, 301→2, 899→2, 900→3, 2699→3, 2700→4, 6499→4, 6500→5
      Two extra edge cases:
        99999→5 (XP above cap stays at level 5)
        computeLevel with any negative XP input (e.g., -1) → 1 (never below level 1)

    describe('xpForNextLevel') — 5 test cases:
      level 1 → 300
      level 2 → 900
      level 3 → 2700
      level 4 → 6500
      level 5 → null

    describe('XP_BY_DIFFICULTY') — 3 test cases (optional but recommended):
      Beginner → 50, Standard → 100, Veteran → 200

    Each it() description must state the input and expected output in plain English
    (e.g., "299 XP returns level 1 — one below L2 threshold").

    Run the tests after writing: npm run test:run -- lib/xp.test.ts
    All tests must pass (GREEN) before this task is marked done.
  </action>
  <verify>
    <automated>npm run test:run -- lib/xp.test.ts</automated>
  </verify>
  <done>lib/xp.test.ts exists with at least 15 test cases; npm run test:run -- lib/xp.test.ts exits 0 with all tests shown as passing; no skipped or failing tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lib/xp.ts → consumers | Pure functions: no input comes from untrusted sources in this plan |
| npm run db:push → Neon DB | Schema push to production database; additive only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | prisma/schema.prisma | mitigate | Schema change is additive (xp/level with defaults); no destructive column removal; db:push requires explicit human confirmation at checkpoint |
| T-02-02 | Information Disclosure | lib/xp.ts | accept | Pure math module; no secrets, no user data; no attack surface |
| T-02-03 | Denial of Service | computeLevel loop | accept | 5-iteration loop over constant array; no unbounded computation; no user-controlled loop count |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan; existing Prisma/Vitest packages already verified in Phase 1 |
</threat_model>

<verification>
After all three tasks complete:

1. Schema: grep -v '^//' prisma/schema.prisma | grep -c "xp.*Int" returns 1
2. Schema: grep -v '^//' prisma/schema.prisma | grep -c "level.*Int" returns 1
3. Type check: npx tsc --noEmit exits 0
4. Tests: npm run test:run -- lib/xp.test.ts exits 0 (all 15+ tests green)
5. Full suite: npm run test:run exits 0 (no regressions in dice tests)
</verification>

<success_criteria>
- prisma/schema.prisma has xp Int @default(0) and level Int @default(1) on the Character model
- npm run db:push applied the schema to the Neon database successfully
- npx prisma generate regenerated the client (xp and level are now typed on Character)
- lib/xp.ts exports XP_THRESHOLDS, XP_BY_DIFFICULTY, computeLevel, xpForNextLevel with zero framework imports
- lib/xp.test.ts contains all 10 required XP-05 boundary cases plus edge cases
- npm run test:run -- lib/xp.test.ts passes with 0 failures
- npm run test:run passes with 0 regressions (dice tests still green)
</success_criteria>

<output>
Create `.planning/phases/02-xp-system/02-01-SUMMARY.md` when done.
</output>
