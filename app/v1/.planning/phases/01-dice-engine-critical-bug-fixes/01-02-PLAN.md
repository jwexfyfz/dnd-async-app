---
phase: 01-dice-engine-critical-bug-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/dice.ts
  - lib/dice.test.ts
autonomous: true
requirements:
  - DICE-02
  - DICE-05

must_haves:
  truths:
    - "lib/dice.ts exports rollDie, rollDice, abilityModifier, proficiencyBonus, rollD20Check"
    - "rollD20Check returns { roll, modifier, total, dc, dcType, success } — dcType is 'AC' or 'DC' per D-05"
    - "All exports are pure functions with zero Prisma imports — DATABASE_URL absence never breaks imports"
    - "lib/dice.test.ts passes all boundary cases for all five exports"
    - "Tests use an injectable rollFn to avoid module mocking — deterministic without vi.spyOn"
  artifacts:
    - path: "lib/dice.ts"
      provides: "rollDie, rollDice, abilityModifier, proficiencyBonus, rollD20Check, D20Result interface"
      exports: ["rollDie", "rollDice", "abilityModifier", "proficiencyBonus", "rollD20Check", "D20Result"]
    - path: "lib/dice.test.ts"
      provides: "DICE-05 unit test coverage for all five exports at boundary conditions"
  key_links:
    - from: "lib/dice.ts"
      to: "lib/prisma.ts"
      via: "NO import — must have zero dependency on prisma"
      pattern: "should NOT match: import.*prisma"
    - from: "lib/dice.test.ts"
      to: "lib/dice.ts"
      via: "named imports"
      pattern: "from './dice'"
---

<objective>
Create lib/dice.ts as the deterministic TypeScript dice engine and lib/dice.test.ts with full unit test coverage, satisfying DICE-02 and DICE-05.

Purpose: This is the core mechanical fix for the "AI invents roll results" bug. Once this exists, take-turn.ts can call rollD20Check before the narration call, making rolls tamper-proof. The injectable rollFn pattern means tests are deterministic without module mocking.
Output: lib/dice.ts (pure functions, zero deps), lib/dice.test.ts (boundary tests for all exports).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-dice-engine-critical-bug-fixes/01-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/research/STACK.md

<interfaces>
From lib/prisma.ts (DO NOT import this — throws at import time without DATABASE_URL):
  throws unconditionally on line 6 if DATABASE_URL is missing.
  lib/dice.ts must have zero imports from lib/prisma.ts.
  Type-only imports from @prisma/client are safe: `import type { Character } from '@prisma/client'`

From lib/ai-config.ts (for reference — do NOT import into dice.ts):
  export const DM_MODEL = "claude-haiku-4-5"
  export const DM_MAX_TOKENS = 600
  export const ROLLING_WINDOW_SIZE = 15

D-05 override (takes precedence over ARCHITECTURE.md signature):
  rollD20Check must return D20Result with dcType: "AC" | "DC" field.
  The ARCHITECTURE.md shows a signature without dcType — CONTEXT.md D-05 wins.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create lib/dice.ts — pure dice engine</name>
  <files>lib/dice.ts</files>
  <read_first>
    - /Users/jeffwang/Desktop/dnd-async-app/.planning/phases/01-dice-engine-critical-bug-fixes/01-CONTEXT.md — D-05 mandates dcType field on D20Result
    - /Users/jeffwang/Desktop/dnd-async-app/.planning/research/ARCHITECTURE.md — module design and export shapes
    - /Users/jeffwang/Desktop/dnd-async-app/lib/prisma.ts — confirm the throw; ensure dice.ts has zero prisma imports
  </read_first>
  <behavior>
    - rollDie(6) returns a value in [1, 6] inclusive
    - rollDie(20) returns a value in [1, 20] inclusive
    - rollDice(3, 6) returns { rolls: number[length=3], total: sum(rolls) }
    - abilityModifier(10) === 0
    - abilityModifier(8) === -1
    - abilityModifier(15) === 2
    - abilityModifier(20) === 5
    - abilityModifier(1) === -5
    - proficiencyBonus(1) === 2
    - proficiencyBonus(4) === 2
    - proficiencyBonus(5) === 3
    - rollD20Check with rollFn=()=>14, modifier=3, dc=14, dcType="AC" returns { roll:14, modifier:3, total:17, dc:14, dcType:"AC", success:true }
    - rollD20Check with rollFn=()=>10, modifier=3, dc=15, dcType="DC" returns { roll:10, modifier:3, total:13, dc:15, dcType:"DC", success:false }
    - rollD20Check with rollFn=()=>20 returns critical:true
    - rollD20Check with rollFn=()=>1 returns fumble:true
  </behavior>
  <action>
    Create lib/dice.ts with these exports — no fenced blocks in this action, names and shapes only:

    Export interface D20Result with fields: roll (number, 1-20 raw d20 face), modifier (number), total (number, roll+modifier), dc (number), dcType ("AC" | "DC"), success (boolean, total >= dc), critical (boolean, roll === 20), fumble (boolean, roll === 1).

    Export rollDie(sides: number): number — returns Math.floor(Math.random() * sides) + 1. No external deps.

    Export rollDice(count: number, sides: number): { rolls: number[]; total: number } — calls rollDie count times, returns array and sum.

    Export abilityModifier(score: number): number — returns Math.floor((score - 10) / 2). This is the standard D&D 5e formula.

    Export proficiencyBonus(level: number): number — table lookup only, no formula: if level >= 5 return 3, else return 2. This covers levels 1-5 per D&D Basic Rules 2014. Do NOT use a formula like Math.ceil(level/4)+1.

    Export rollD20Check(modifier: number, dc: number, dcType: "AC" | "DC", rollFn: () => number = rollDie.bind(null, 20)): D20Result — calls rollFn() to get the raw roll, computes total = roll + modifier, returns full D20Result. The rollFn parameter with default makes this testable without module mocking.

    Zero imports from lib/prisma.ts or lib/supabase-server.ts. No framework dependencies. Pure TypeScript only.

    Use the existing `// ─── Section ───` separator style from lib/ai-config.ts for any section dividers.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <done>lib/dice.ts compiles without TypeScript errors. All six exports present. No prisma import exists in the file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create lib/dice.test.ts — boundary unit tests (DICE-05)</name>
  <files>lib/dice.test.ts</files>
  <read_first>
    - /Users/jeffwang/Desktop/dnd-async-app/lib/dice.ts — read the finished implementation to write tests against its exact signatures
    - /Users/jeffwang/Desktop/dnd-async-app/.planning/research/PITFALLS.md — T1 (inject rollFn, don't mock), T4 (proficient vs non-proficient paths)
    - /Users/jeffwang/Desktop/dnd-async-app/.planning/research/STACK.md — globals: false means explicit vitest imports required
  </read_first>
  <behavior>
    rollDie tests:
    - rollDie(6) called 100 times always returns values in [1,6]
    - rollDie(20) called 100 times always returns values in [1,20]
    - rollDie(1) always returns 1

    rollDice tests:
    - rollDice(3,6): rolls.length === 3, total === rolls.reduce((a,b)=>a+b,0)
    - rollDice(1,1): rolls=[1], total=1

    abilityModifier boundary tests (per REQUIREMENTS.md DICE-02):
    - score 1  → -5
    - score 8  → -1
    - score 9  → -1 (borderline: (9-10)/2 = -0.5, floor = -1)
    - score 10 → 0
    - score 11 → 0
    - score 12 → 1
    - score 15 → 2
    - score 20 → 5

    proficiencyBonus tests:
    - level 1 → 2
    - level 2 → 2
    - level 3 → 2
    - level 4 → 2
    - level 5 → 3

    rollD20Check tests (all using injected rollFn — no vi.spyOn):
    - rollFn=()=>14, modifier=3, dc=14, dcType="AC": total=17, success=true, critical=false, fumble=false, dcType="AC"
    - rollFn=()=>10, modifier=3, dc=15, dcType="DC": total=13, success=false, critical=false, fumble=false, dcType="DC"
    - rollFn=()=>14, modifier=0, dc=15, dcType="DC": total=14, success=false (boundary: exactly one below DC)
    - rollFn=()=>15, modifier=0, dc=15, dcType="DC": total=15, success=true (boundary: exactly at DC)
    - rollFn=()=>20, modifier=0, dc=25, dcType="DC": critical=true, success=false (nat 20 does NOT auto-succeed on skill checks per pitfall L4)
    - rollFn=()=>1, modifier=10, dc=5, dcType="DC": fumble=true (fumble flag even when total would succeed)
    - All returned dcType values match the input dcType parameter
  </behavior>
  <action>
    Create lib/dice.test.ts co-located beside lib/dice.ts. Use explicit vitest imports: `import { describe, it, expect } from 'vitest'` (globals: false in vitest.config.ts). Import only named exports from './dice'.

    Organize tests using describe blocks: "rollDie", "rollDice", "abilityModifier", "proficiencyBonus", "rollD20Check".

    For rollDie bounds tests: run in a loop 100 iterations, assert each result is >= 1 and <= sides. This tests the full range without mocking Math.random.

    For rollD20Check: pass the injected rollFn parameter directly — no vi.spyOn, no vi.mock. Example call pattern: rollD20Check(3, 14, "AC", () => 14).

    Critical: include the nat-20-does-not-auto-succeed test case (rollFn=()=>20, dc=25 → success=false). This guards against a common incorrect implementation.

    Use "─── Section ───" comment style if you add section breaks. Keep test descriptions specific and concrete.
  </action>
  <verify>
    <automated>npm run test:run -- lib/dice.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>npm run test:run passes all tests in lib/dice.test.ts with zero failures. Output shows each describe block passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lib/dice.ts → Math.random() | Randomness source for all dice rolls |
| rollD20Check dcType parameter | Caller-supplied string that flows into D20Result and eventually Claude's narration prompt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Math.random() dice results | accept | Math.random() is sufficient for game use; no cryptographic randomness needed; results are deterministic in tests via injectable rollFn |
| T-02-02 | Tampering | dcType parameter on rollD20Check | mitigate | TypeScript type "AC" \| "DC" enforced at compile time; take-turn.ts is the only caller and it sets dcType based on actionType — not user-supplied input |
| T-02-03 | Information Disclosure | D20Result exposed to narration call | accept | Roll results are intentionally passed to Claude's narration prompt (D-07); Claude is explicitly forbidden from changing outcomes via the system prompt |
</threat_model>

<verification>
1. `npm run test:run -- lib/dice.test.ts` exits 0 with all tests passing.
2. `grep -c "import.*prisma" lib/dice.ts` returns 0 (no prisma import).
3. `grep "dcType" lib/dice.ts | grep -c '"AC" | "DC"'` returns > 0 (dcType field typed per D-05).
4. `grep -c "rollD20Check" lib/dice.test.ts` returns > 5 (multiple test cases).
5. `grep -c "vi.spyOn\|vi.mock" lib/dice.test.ts` returns 0 (no module mocking — injectable rollFn used instead).
6. `grep -c "() => 20" lib/dice.test.ts` returns > 0 (nat-20 critical flag test present).
</verification>

<success_criteria>
- lib/dice.ts: all five exports present, D20Result has dcType field, zero prisma imports, compiles cleanly
- lib/dice.test.ts: all boundary cases pass for all five exports, rollFn injection used throughout, no vi.spyOn
- `npm run test:run` passes with no failures
- Natural 20 does not auto-succeed a skill check (dc=25, roll=20 → success=false)
- proficiencyBonus(5) === 3 and proficiencyBonus(4) === 2 (step-function boundary)
</success_criteria>

<output>
Create `.planning/phases/01-dice-engine-critical-bug-fixes/01-02-SUMMARY.md` when done.
</output>
