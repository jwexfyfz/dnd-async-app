---
context: research
phase: gamemap-phase-c
task: C4
total_tasks: 10
status: in_progress
last_updated: "2026-05-29T16:41:47.667Z"
---

# GameMap Phase C — Code Updates

No blocking constraints. No anti-patterns encountered.

<current_state>
Phase A (schema migration) and Phase B (backfill) are complete and verified.
Phase C (code updates) is in progress — 7 of 10 tasks done.
`get-map-items.ts` was just rewritten this session.
Next: fix `auto-advance.ts` and `take-turn.ts` (the two largest remaining files).

Key schema facts:
- `Enemy` NO LONGER HAS: currentHp, posX, posY, isHiding, status, stealthRoll, hasReaction, isSurprised
- `Item` NO LONGER HAS: mapId, posX, posY, activeFromSceneId, enemyId
- `Game` NO LONGER HAS: mapId
- `GameInventory` model is deleted
- `GameMap` model exists with `data Json` containing the full per-game map state
- `GameMap.data` structure: { width, height, tiles, playerStart, rooms, pois, items[], enemies[] }
- `GameMap.data.enemies[]`: { enemyId, posX, posY, currentHp, maxHp, status, isHiding, stealthRoll, hasReaction, isSurprised, lootItemIds }
- `GameMap.data.items[]`: { itemId, posX, posY, isPickedUp, isVisible, activeFromSceneOrder, droppedByEnemyId }
</current_state>

<completed_work>

- C1: lib/chip-candidates.ts — no changes needed (pure function)
- C2: lib/scene-advance.ts — already reads from GameMap.data correctly
- C7: components/turn-queue-sheet.tsx — no map/enemy reads, no changes
- C8: hooks/useTurnQueue.ts — no map/enemy reads, no changes
- C9: GameInventory cleanup — zero references found
- C10: lib/create-game-map.ts — already implemented
- app/actions/get-map-items.ts — REWRITTEN: now accepts `gameId` string, queries GameMap.data for ground items + character FK relations for equipped/backpack items
</completed_work>

<remaining_work>

**C4: app/actions/auto-advance.ts** (most complex — do this first)
Changes needed in order:
1. Remove `map: { include: { items: { select: { ... posX, posY } } } }` from game query (line ~251)
2. After loading `game`, add:
   ```typescript
   const activeGameMap = game.currentActId
     ? await prisma.gameMap.findUnique({
         where: { gameId_actId: { gameId, actId: game.currentActId } },
         select: { id: true, data: true },
       })
     : null;
   const gmData = (activeGameMap?.data ?? {}) as Record<string, any>;
   const mapData = gmData;
   ```
3. Remove `const mapData = game.map.data as Record<string, any>;` (line ~272)
4. Fix enemy pre-load (~lines 278-288): Remove `currentHp, posX, posY` from select; read from `gmData.enemies` instead
5. Fix `dbEnemies` query (~lines 302-326): Replace Enemy.findMany with currentHp/posX/posY with:
   - Get gmEnemiesArr from `gmData.enemies`
   - Query Enemy templates for `id, name, maxHp` only
   - Merge: `{ id, name, currentHp: gm?.currentHp ?? maxHp, maxHp, posX: gm?.posX ?? 0, posY: gm?.posY ?? 0 }`
6. Fix `hiddenEnemyIds` (~lines 330-338): Remove `prisma.enemy.findMany({ isHiding: true })`; read from `gmData.enemies[].isHiding`
7. Fix mapItems for prompt (~line 369): Replace `(game.map as any).items` with items from `gmData.items` + Item template join for names
8. Fix enemy HP resolution in transaction (~lines 579-599):
   - Remove `tx.enemy.findMany({ select: { currentHp } })` → read from GameMap.data
   - Remove `tx.enemy.update({ data: { currentHp } })` → write to GameMap.data.enemies via `tx.gameMap.update`
9. Fix item pickup (~lines 625-659):
   - Remove `tx.item.findUnique({ select: { posX, posY } })`
   - Remove `tx.item.update({ data: { posX: null, posY: null } })`
   - Instead: find item in `GameMap.data.items`, mark `isPickedUp: true`, write back via `tx.gameMap.update`
10. Fix `checkSceneTrigger` calls (~lines 668, 993): Add `gameId, currentActId: game.currentActId ?? null`
11. Fix scene enemy loading on transition (~line 676): Remove `currentHp, posX, posY` from enemy select; use `gmData.enemies` for HP/pos
12. Fix act transition (~lines 693-743):
    - Remove `mapId: nextMap.id` from `tx.game.update`
    - Add `import { createGameMap } from "../../lib/create-game-map";` at top
    - Add `await createGameMap(gameId, nextAct.id, tx);` inside the transaction after updating the game

**C5: app/actions/take-turn.ts** (same pattern as C4 plus extras)
- Same steps 1-11 as C4
- Extra: Fix AoO `dbEnemies` query (~line 493): Remove `posX, posY` from Enemy select; read from `gmData.enemies`
- Extra: Fix `encounterCompleted` check (~line 905): Replace `tx.enemy.findMany({ select: { currentHp } })` with GameMap.data read

**C3: app/actions/handle-player-action.ts**
- Review only — likely no changes (no map include, no enemy writes, no item writes)

**C6: app/game/[id]/page.tsx + sub-components**
- Line ~289: Change `getMapItems(gameData.map.id)` → `getMapItems(gameData.id)`
- PartyTab: Change `mapId={gameData.map.id}` → `gameId={gameData.id}` (and update prop name/type)
- MemberInventoryPane: Change `mapId: string` prop → `gameId: string`; update useEffect call
- TotalACCard: Change `mapId: string` prop → `gameId: string`; update useEffect call
- MemberStatsPane: Pass `gameId` instead of `mapId` to TotalACCard
</remaining_work>

<decisions_made>

- `getMapItems(mapId)` → `getMapItems(gameId)`: Item.mapId is removed; items now tracked in GameMap.data
- Enemy HP/position reads come from `GameMap.data.enemies`, not Enemy table rows
- Enemy HP writes go to `GameMap.data.enemies[].currentHp` via `gameMap.update` inside transaction
- Item pickup marks `GameMap.data.items[].isPickedUp = true` instead of `Item.posX = null`
- `checkSceneTrigger` already accepts `gameId` + `currentActId` in its interface — callers just weren't passing them
- `lib/create-game-map.ts` already exists and accepts an optional `tx` for transaction use
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order)
1. `.planning/GAMEMAP_REQUIREMENTS.md` — full spec for all phases; Phase C checklist is the task list
2. `prisma/schema.prisma` — verify exact current field names before writing queries
3. `lib/create-game-map.ts` — understand the helper signature before using it in act transition

## Infrastructure State
- DB migration applied: `20260529120000_add_game_map_and_cleanup`
- Prisma client regenerated
- 2 existing games backfilled with GameMap records (verified)
- App not running during this session

<context>
We're doing GameMap Phase C: updating all server-side code to read/write from GameMap.data instead of the now-removed Enemy/Item DB columns. The schema migration is done, backfill is done. The remaining work is purely TypeScript code fixes — no new DB changes needed. The two largest files (auto-advance.ts at 782 lines and take-turn.ts at 1043 lines) need the most changes but follow a consistent pattern. Once those compile, we run `npm run build` to verify zero TS errors (Phase E task E1).
</context>

<next_action>
Open `app/actions/auto-advance.ts`. Make changes in the order listed under C4 above. Use Edit tool for surgical replacements. The first edit: remove `map: { include: { items: { select: ... } } }` from the Prisma game query and add the GameMap load block immediately after `const game = await prisma.game.findUnique(...)`.
</next_action>
